# packaging
hatchling
wheel

# core dependencies
-e .

# testing + development
#
# until pip adds support for dependency groups, we use the
# append any dependencies below for testing and development.
tox >= 4.21 # tox is a task runner
tox-pyenv-redux # tox plugin for pyenv
ipython # ipython is a REPL for Python
ipdb # ipdb is a debugger for Python using IPython

# linting
ruff ~= 0.8 # ruff is a linter for Python